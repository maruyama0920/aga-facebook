# Facebook Messenger ボット — 全体像と申請時の観点

## 1. ざっくりした仕組み

1. ユーザーが Messenger でページ（ボット）にメッセージ／ボタン操作をする  
2. Meta が `POST /webhook` にイベントを送る（開発者サーバーは Express）  
3. アプリが Graph API（`/me/messages`）で返信する（テキスト、画像、テンプレートなど）  
4. 必要に応じて Gmail 等でメール通知する  

Webhook の検証は `GET /webhook`（`hub.verify_token`）、本番では `X-Hub-Signature-256` による署名検証を有効にする。

## 2. リポジトリ内の役割

| もの | 役割 |
|------|------|
| `src/index.ts` | Webhook、診断フロー、Send API 呼び出し |
| `env.yaml` / 環境変数 | ページアクセストークン、検証トークン、App Secret、メールなど |
| `Dockerfile` / `cloudbuild.yaml` | コンテナ化・デプロイ |

## 3. 画像付きの選択肢・結果（実装方針）

Messenger の制約上、**画像とボタンを同じバブルに載せる**には **ジェネリックテンプレート**（`template_type: generic`）の要素に `image_url` と最大 3 個の `postback` ボタンを載せる方法が一般的です。  
**Quick Reply には画像を付けられません。**

- 画像 URL は **HTTPS** で、認証なしで取得できる公開 URL にする（自前 CDN・クラウドストレージ・ページにアップロードした画像の URL 等）。  
- 診断「結果」で長文を送る場合は、**先に画像のみ送る**（`attachment.type: image`）→ 続けてテキスト、という並びもよく使われます。  

環境変数で **HTTPS の公開 URL** を指定（未設定なら従来どおりテキスト＋ボタンのみ）。

| 変数名 | 用途 |
|--------|------|
| `IMAGE_WELCOME` | 診断スタートのカード |
| `IMAGE_Q1` … `IMAGE_Q6` | 各質問カード |
| `IMAGE_RESULT_CTA` | 「診断結果をみる」直前のカード |
| `IMAGE_RESULT_PRIVACY` / `IMAGE_RESULT_EFFECT` / `IMAGE_RESULT_PRICE` | 各診断結果の冒頭ヒーロー画像（続けてテキスト） |
| `IMAGE_COUNSELING` | カウンセリング誘導カード |
| `IMAGE_MORE_INFO` | 「もっと知りたい」後の予約ボタンカード |

`SUMMARY.txt` も参照。

## 4. Meta（Facebook）アプリ申請・審査で揃えたいもの

審査要件は随時変わるため、**必ず [開発者向けドキュメント](https://developers.facebook.com/docs/messenger-platform/overview) と App Dashboard の該当画面**で最新を確認する。

一般的に必要になりやすいもの:

1. **ビジネス認証・アプリの公開設定**  
   - 利用する製品（Messenger 等）を有効化し、ページとアプリを接続する。  

2. **`pages_messaging`（および必要なら関連権限）**  
   - ユーザーにメッセージを送るために、標準アクセスからの利用には審査が必要なことが多い。  

3. **プライバシーポリシー URL**  
   - データの取り扱い（会話ログ、メール送信、保存期間など）を記載した公開ページ。  

4. **利用規約・ブランド**  
   - 必要に応じて利用規約、問い合わせ先。  

5. **スクリーンキャスト**  
   - 審査担当が再現できるよう、**エンドツーエンドの操作**（友だち追加〜診断〜結果〜問い合わせまで）を動画で提示。  

6. **本番相当の動作**  
   - テストユーザーまたは実アカウントで、申請内容どおりにボットが動くこと。  
   - 署名検証を本番では有効にする（`SKIP_SIGNATURE_VERIFICATION` は開発限定）。  

7. **コンテンツポリシー**  
   - 医療・景表に触れる文言は、広告表示基準や業界ガイドラインに合わせて法務確認することが望ましい。  

## 5. 運用上の注意

- **メモリ上のユーザー状態**（`Map`）はプロセス再起動で消える。スケールアウト時は共有ストアが必要。  
- **ページアクセストークン**の期限・権限を定期的に確認。  
- ログに個人情報（電話番号など）を出しすぎないよう注意。  

## 6. 変更履歴（このドキュメント）

| 日付 | 内容 |
|------|------|
| 2026-04-14 | LINE 向け旧稿を廃止し、Facebook Messenger 向けに差し替え |
